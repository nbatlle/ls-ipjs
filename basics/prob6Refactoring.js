console.log(Number('5') + 10);
/* Assuming there is a good reason for the first expression to be '5' rather than just 5,
the value can be explicitly coerced to a number using the Number() function.  Alternatively,
parseInt() could be used. */
console.log(parseInt('5') + 10);

